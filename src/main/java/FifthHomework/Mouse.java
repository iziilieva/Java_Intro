package FifthHomework;

public class Mouse extends Pet {
    public Mouse(boolean hasTail, String name, int age, PetBreed breed, PetAddress address, int barcode) {
        super(hasTail, name, age, breed, address, barcode);
    }
}
